{
  "name": "msp-homelab-pathway",
  "version": "0.1.0",
  "private": true,
  "description": "A 52-week homelab curriculum platform for MSP technicians. Free-first, artifact-verified.",
  "license": "MIT",
  "author": "anthonyjohnsonga",
  "repository": {
    "type": "git",
    "url": "https://github.com/anthonyjohnsonga/MSP-Homelab-Pathway.git"
  },
  "workspaces": [
    "shared",
    "web",
    "api",
    "cli"
  ],
  "scripts": {
    "validate": "npm run validate --workspace @pathway/shared",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace @pathway/web",
    "build": "npm run build --workspace @pathway/shared && npm run build --workspace @pathway/web"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
