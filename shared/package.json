{
  "name": "@pathway/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Curriculum types, loader, validation and helpers shared by web, api and cli.",
  "license": "MIT",
  "author": "anthonyjohnsonga",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "validate": "node src/bin/validate.ts ../data/curriculum.json",
    "test": "node --test \"test/**/*.test.ts\"",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
